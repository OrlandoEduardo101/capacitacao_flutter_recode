import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:desafio_recode/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // GithubController github;
  //
  setUp(() {
    //     github = HomeModule.to.get<GithubController>();
  });

  group('GithubController Test', () {
    //   test("First Test", () {
    //     expect(github, isInstanceOf<GithubController>());
    //   });

    //   test("Set Value", () {
    //     expect(github.value, equals(0));
    //     github.increment();
    //     expect(github.value, equals(1));
    //   });
  });
}
